# Autodelegate Rewards (Agoric TestNet)
Simple script to auto delegate the rewards/commissions to gain power on Agoric Testnet

## Configure
Change var values to yours
Make it executable:
`chmod +x autodelegate_rewards.sh`

## Run
Execute a screen session
`screen -S Autodelegate`

Run in a loop:
`./autodelegate_rewards.sh`

